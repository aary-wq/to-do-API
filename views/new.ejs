<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Task</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #ff6b6b, #ffe66d, #4ecdc4);
            background-size: 300%;
            margin: 0;
            padding: 20px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            color: #fff;
            font-size: 2.8rem;
            font-weight: 700;
            letter-spacing: 1.5px;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
            margin-bottom: 30px;
            position: relative;
            text-align: center;
        }

        h1::after {
            content: '';
            width: 100px;
            height: 4px;
            background: #fff;
            display: block;
            margin: 10px auto;
            border-radius: 2px;
        }

        form {
            background: linear-gradient(145deg, rgba(255, 255, 255, 0.95), rgba(240, 240, 240, 0.9));
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
            width: 100%;
            max-width: 500px;
            display: flex;
            flex-direction: column;
            gap: 15px;
            transition: transform 0.3s ease;
        }

        form:hover {
            transform: translateY(-5px);
        }

        input[type="text"],
        textarea {
            font-family: 'Poppins', sans-serif;
            font-size: 1rem;
            padding: 10px;
            border: 2px solid #e2e8f0;
            border-radius: 10px;
            outline: none;
            transition: border-color 0.3s ease;
        }

        input[type="text"]:focus,
        textarea:focus {
            border-color: #6366f1;
        }

        textarea {
            resize: vertical;
        }

        .file-upload-wrapper {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
        }

        label.custom-upload {
            display: inline-block;
            background: linear-gradient(45deg, #6366f1, #a5b4fc);
            color: white;
            padding: 10px 20px;
            border-radius: 25px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 500;
            text-align: center;
            transition: all 0.3s ease;
        }

        label.custom-upload:hover {
            background: linear-gradient(45deg, #4f46e5, #818cf8);
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(99, 102, 241, 0.3);
        }

        input[type="file"] {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0;
            cursor: pointer;
        }

        .file-name {
            font-size: 0.9rem;
            color: #2d3748;
            font-weight: 500;
        }

        button {
            background: #3b82f6;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 25px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 500;
            transition: all 0.3s ease;
            align-self: center;
        }

        button:hover {
            background: #2563eb;
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(59, 130, 246, 0.3);
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2rem;
            }

            form {
                padding: 15px;
            }

            input[type="text"],
            textarea {
                font-size: 0.9rem;
            }

            label.custom-upload,
            button {
                padding: 10px 15px;
                font-size: 0.9rem;
            }

            .file-name {
                font-size: 0.8rem;
            }
        }
    </style>
</head>
<body> 
<form method="post" action="/tasks" enctype="multipart/form-data">
 <h1>Create New Task</h1> 
 <input type="text" placeholder="Enter task name:" name="task" required>
 <textarea placeholder="enter description" cols="30" rows="12" name="desc" required></textarea>  
 <div class="file-upload-wrapper">
     <label for="fileUpload" class="custom-upload">Upload Image:</label>
     <input type="file" name="img" id="fileUpload" required> 
     <span class="file-name">No file selected</span>
 </div>
 <button>Submit</button>
</form>
<script>
    document.getElementById('fileUpload').addEventListener('change', function() {
        const fileName = this.files[0]?.name || 'No file selected';
        document.querySelector('.file-name').textContent = fileName;
    });
</script>
</body>
</html>